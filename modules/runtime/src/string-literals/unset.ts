import type { Unset } from "inferred-types/types";

/**
 * the runtime token of the type `Unset` which is meant to delineate
 * a value which is in it's default state (aka, has not been set).
 */
export const unset = { kind: "Unset" } as Unset;
