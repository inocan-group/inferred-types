/**
 * **TypeStrength**
 *
 * Some types provide a generic which allows users to choose
 * between a "strong but complex" versus a "simpler but effective"
 * typing.
 */
export type TypeStrength = "strong" | "simple";
