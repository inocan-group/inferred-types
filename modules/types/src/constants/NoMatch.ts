/**
 * A type used in **take** functions and lexers to indicate that _what_
 * the take function is configured to look for in the HEAD of the string
 * being parsed _was not_ found.
 */
export type NoMatch = "NO MATCH";
