/**
 * Static text blocks which can be used to interpolate into dynamic
 * string literal sections.
 */
export const INTERPOLATION_BLOCKS = [
    "{{string}}",
    "{{number}}",
    "{{boolean}}"
] as const;
