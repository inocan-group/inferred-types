name: Testing
on:
    push:
        branches:
            - main
jobs:
    test:
        name: testing
        uses: yankeeinlondon/gha/.github/workflows/test.yml@main
        permissions:
            contents: read
            id-token: write
