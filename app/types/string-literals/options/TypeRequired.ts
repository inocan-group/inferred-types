
/**
 * **TypeRequired**
 * 
 * Some types provide a generic which indicate whether the type should be
 * considered _optional_, _required_, or just fully _excluded_.
 */
export type TypeRequired = "required" | "optional" | "exclude";
