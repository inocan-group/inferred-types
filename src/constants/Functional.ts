/**
 * **RESULT**
 * 
 * Concrete values for `Ok` and `Err` states.
 */
export const RESULT = {
  Ok: "ok",
  Err: "err"
} as const;


/**
 * **OPTION**
 * 
 * Concrete values for `Some` and `None` states.
 */
export const OPTION = {
  Some: 1,
  None: 0
} as const;
