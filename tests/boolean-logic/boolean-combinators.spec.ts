import { describe, it,  } from "vitest";
// import type { Equal, Expect } from "@type-challenges/utils";

describe.skip("boolean logic", () => {
  it("AND operation", () => {
    // const a1 = and(
    //   () => true,
    //   () => false
    // );
    // const a2 = and(
    //   () => true,
    //   () => true
    // );
    // const md = and(createFilter({ endsWith: ".md" }), createFilter({ not: { startsWith: "_" } }));

    // expect(a1()).toBe(false);
    // expect(a2()).toBe(true);
    // expect(["foo.md", "bar.html", "_baz.md"].filter(md)).toEqual(["foo.md"]);
  });
});
