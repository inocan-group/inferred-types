import { describe, it } from "vitest";

describe.skip("createMatchTemplate()", () => {

  it("solo template", () => {
    type cases = [
      /** type tests */
    ];
  });


  it("multi template", () => {
    type cases = [
      /** type tests */
    ];
  });


});
